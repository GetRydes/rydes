import { ICustomerSchema } from "../../types";

const makeRetrieveCustomer = ({ customerSchema }: ICustomerSchema) => {
  return async (customerInfo) => {
    // const user = makeUser
  };
};

export default makeRetrieveCustomer;
